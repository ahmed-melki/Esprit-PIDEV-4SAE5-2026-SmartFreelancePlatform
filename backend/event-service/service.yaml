apiVersion: v1
kind: Service
metadata:
  name: gestion-event-service
  namespace: default
spec:
  selector:
    app: gestion-event
  ports:
    - port: 8081
      targetPort: 8081
      nodePort: 30081
  type: NodePort