apiVersion: v1
kind: Service
metadata:
  name: gestion-blog-service
  namespace: default
spec:
  selector:
    app: gestion-blog
  ports:
    - port: 8082
      targetPort: 8082
      nodePort: 30082
  type: NodePort