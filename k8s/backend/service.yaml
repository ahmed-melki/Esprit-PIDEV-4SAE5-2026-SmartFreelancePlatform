apiVersion: v1
kind: Service
metadata:
  name: project-offer-backend
  namespace: project-offer
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: project-offer-backend