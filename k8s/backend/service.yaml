apiVersion: v1
kind: Service
metadata:
  name: project-offer-backend
  namespace: project-offer
spec:
  type: ClusterIP
  ports:
  - port: 8045          
    targetPort: 8045      
  selector:
    app: project-offer-backend